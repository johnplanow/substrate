# Acceptance Gate Program — Ledger

**This file is the cross-session state machine.** Read it at session start; update it with every status change; ship ledger updates in the same commit as the code they describe. Statuses: `todo` | `in-progress (YYYY-MM-DD)` | `done (vX.Y.Z)` | `blocked` | `dropped (reason)`.

Program start: 2026-07-07. Plan: `execution-plan.md` (same dir). Design: `_planning/2026-07-07-acceptance-gate-design-brief.md` (rev 2). Ground truth: `_planning/2026-07-04-income-sources-field-feedback.md` + the income-sources acceptance review (pinned by A3.1).

## Status board

| ID | Story | Ship | Status | Version | Evidence / notes |
|----|-------|------|--------|---------|------------------|
| A0.1 | Registry schema + trusted-tree loader | 1 | done (v0.21.2) | v0.21.2 | packages/sdlc/src/acceptance/ (types/registry/loader): zod schema w/ pathed issues, `git show` trusted-tree loader (array-argv, absent≠invalid≠error). Tests: registry 11, loader 10 incl. H7-SEMANTICS (worktree tamper invisible; uncommitted-only registry = absent). CLI `substrate acceptance validate [--ref]` live on BUNDLED dist: ok/absent/invalid/tamper-divergence all verified, exit codes correct. BONUS FIX: gitignore writer now negates `!.substrate/acceptance/` (+repair pass) — without it the registry could never be committed and the trusted loader would report absent FOREVER (H1.1 profile-vanish class); verified live via git check-ignore post-init |
| A0.2 | Create-story journey tags | 1 | done (v0.21.2) | v0.21.2 | {{journey_registry}} prompt var (trusted-tree load in create-story, parentProjectRoot); `journeys:` frontmatter (tolerant schema, .catch isolation — malformed tag cannot drop external_state_dependencies, test proves); unknown id → schema_validation_failed naming ids. Tests: create-story-journeys 7 (real tmp git repos, incl. H7 worktree-tamper-invisible prompt test), journey-frontmatter 6. LIVE SMOKE (real claude dispatch, uv fixture, committed registry): agent emitted `journeys: [UJ-1]` frontmatter unprompted-by-operator, full cycle green (create→dev→verify→merge, post-merge pytest green), tag validation passed. Eval gate REGRESSION GREEN 100% (35/35) |
| A0.3 | Epic-close coverage audit + escalations | 2 | done (v0.21.3) | v0.21.3 | `computeJourneyCoverage` pure ledger (12 unit tests, all 5 states × precedence: deferred > fail > pass > unwalked/unclaimed); claims persisted to per-story state at create-story success + artifact-reuse path; epic-close audit at the finalization choke point (blocking → last story escalates `journey-unclaimed`/`journey-unwalked` BEFORE integrating; advisory default warns) + run-end final sweep persisting manifest `journeys[]`; `acceptance.mode` config (both schema copies, all 3 orchestrator threading sites); `acceptance:coverage` NDJSON event (event-types + union + help-agent + template); `substrate acceptance defer` CLI (live: defer/dup/unknown-id paths on bundled dist). 6 orchestrator-integration tests (incl. mode-off + deferral honored) + harness leak fix (checkGitDiffFiles impl leak). **MATRIX CELL `journey-unclaimed` PASS live**: committed registry + blocking + untagged story → escalated, no merge, branch durable, coverage event unclaimed — the UJ-2 class caught structurally on every CI run |
| A1.1 | `acceptance:` profile contract (injection-safe placeholders) | 3 | done (v0.21.4) | v0.21.4 | `AcceptanceContractSchema` (acceptance: block in project profile; strict, pathed issues; profile schema passthrough with no-dual-schema note); `buildRenderArgv` splits BEFORE substitution — 5 hostile-value shapes proven literal tokens (unit) + live child-argv proof (render test); unknown placeholder = HARD error. Trusted-tree loader + H7 tamper test (real git). `acceptance-unrunnable`: registry-present + contract-absent/invalid OR invalid registry → blocking escalates on last story of epic (3 integration tests), precedence unclaimed > unrunnable > unwalked keeps the UJ-2 signal most specific. CLI `validate` reports contract status (live on bundled dist: OK + ABSENT paths) |
| A1.2 | Render executor (external artifacts dir, forensics, determinism probe) | 3 | done (v0.21.4) | v0.21.4 | `renderSurface`: argv spawn (no shell), H4.1-mirror env scrub + git ceiling, process-GROUP kill on timeout, stdout captured as first-class artifact (CLI surfaces), exit+stderrTail forensics (H0.4 parity), never-throws. `renderSurfaceDeterministic`: dual render + tree hash-compare (retry-never-on-render rule). 8 REAL-spawn tests incl. live injection proof (hostile artifacts path = one literal argv token, no PWNED file) + timeout-kill + divergence detection. Pipeline wiring of render+events lands with the stage (A2/A4) — event names already registered |
| A1.3 | Spec-tamper + fixture-mutation tripwires | 4 | done (v0.21.5) | v0.21.5 | `AcceptanceSpecCheck` (Tier A): worktree-vs-trusted compare of journeys.yaml + deferrals.yaml (edit/DELETE/INTRODUCE all fail `acceptance-spec-tampered`) + profile acceptance-BLOCK fingerprint (render-command laundering; non-acceptance profile edits explicitly not flagged) + `acceptance-fixture-mutation` warn on declared-fixtures diffs. Guard armed only when the TRUSTED tree has a registry (arming unconditionally turned mocked-fs test envs into false fails — epic-25 catch). 9 unit tests incl. no-false-positive + prefix-similarity cases. **MATRIX CELL `spec-tamper` PASS live** (pulled forward from A2.3): stub weakens worktree end-state → verification FAIL, no merge, branch durable. Closes the A0.3 carry-over (deferrals introduce-vector covered) |
| A2.1 | Judge dispatch (separate lineage, UNREACHABLE first-class, evidence mandatory) | 5 | done (v0.21.6) | v0.21.6 | New taskType `acceptance-judge` (worktree-coupled set; cwd = external artifacts dir); `packs/bmad/prompts/acceptance-judge.md` (separate lineage; artifact-content-is-DATA injection posture; evidence mandatory incl. PASS; UNREACHABLE = strongest finding); `AcceptanceJudgeResultSchema` (evidence schema-mandatory, verdict case-normalized); `runAcceptanceJudge` (artifact contents inlined capped 16k/artifact; coverage validation: missing/unknown/duplicate end-state ids; ONE corrective retry → `acceptance-judge-invalid`; refusal → `acceptance-judge-refused`, no retry). 8 unit tests incl. SEPARATE-LINEAGE pin (prompt has no files_modified/storyKey; params cannot carry a diff by construction). Prompt-edit smoke deferred to A2.3 (no pipeline invocation path exists yet — live judge dispatch is A2.3 evidence) |
| A2.2 | Verdict artifact + report + notifications + events | 5 | done (v0.21.6) | v0.21.6 | `renderVerdictHtml` (self-contained page, journey × end-state table, UNREACHABLE flagged "never wired", XSS-escape test for hostile excerpts); events `acceptance:started/rendered/verdict` registered end-to-end (bus types + NDJSON interfaces + union + EVENT_TYPE_NAMES + run.ts mapping + help-agent metadata); manifest journeys[] entries gain verdicts[] + run-level acceptance_report_path; `substrate report` renders "Acceptance — journey coverage" section (human + JSON `acceptance` field). Emission wiring = A2.3 stage |
| A2.3 | Acceptance STAGE wiring + matrix cells | 6 | done (v0.21.7) | v0.21.7 | THE LOOP CLOSED: `runAcceptanceStage` in the finalization path (post-verification, pre-audit) — renders claimed journeys' surfaces from the worktree via the trusted contract into `~/.substrate/acceptance/<name>-<hash8>/<run>/<story>/<journey>/`, dispatches the separate-lineage judge, records verdicts into the coverage audit (journey pass = ALL end-states PASS) + manifest (verdicts[] + acceptance_report_path + verdict HTML written at run-end sweep) + acceptance:started/rendered/verdict events. Stub judge branch (parses end-state ids from prompt) + journey-tagging create-story stub. Cells LIVE: `journey-pass` (render→judge→walked-pass→merge), `journey-unreachable` (UNREACHABLE verdicts→walked-fail; blocking = A4.1), `acceptance-unrunnable` (claimed+blocking+no contract→escalated, no merge). All 5 planned cells now exist (journey-unclaimed Ship 2, spec-tamper Ship 4). Matrix 21 cells. LIVE REAL-AGENT JUDGE smoke: real dispatch → real render (`uv run python render_cli.py` in the worktree) → real claude judge → `{"end_state_id":"UJ-1.a","verdict":"PASS","artifact":"cli/cli-stdout.txt","excerpt":"Goodbye, world!"}` → coverage walked-pass → merged (Ship 5 smoke debt paid). Smoke script's unrelated assert tripped on the known H5.5 live shape (fix agent deleted the story artifact post-merge) — not an A2.3 regression, recovery worked as designed |
| A3.1 | income-sources retro-fit corpus pin (pre/post-fix SHAs + 5 misses) | 7 | done (repo-only, no version) | — | `retrofit/CORPUS.md`: TWO-episode pin (no single SHA has all 5 misses — UJ-2 taps fixed at `689b83e` BEFORE the review; pre-fix pins = `ef1c0c8` for miss 1, `a6ff1ca` for misses 2–5; post-fix = `82f4fe7`, tree ≡ fix commit `8d115d7`). All 5 misses documented with file locations + PRD-observable end-states; review doc + PRD §2.3 journeys + render commands (`machine packet compose` / `pre-claim --run-once`) pinned. KEY FIND: the review's own golden render snapshots exist IN-REPO at both pins (`acceptance-renders/` pre, `/v2` post) — A3.2 can judge real artifacts via a file-surface contract without seeding a scratch ledger. A3.2 integrity rule written into the protocol: every end-state must cite its PRD sentence |
| A3.2 | Retro-fit gate run: 5/5 pre-fix detections, 0 post-fix false FAILs | 7–8 | **done (2026-07-08) — 5/5 · 0 FALSE FAILS · 0 PROMPT ITERATIONS** | — | THE GATE'S OWN DoD MET (`retrofit/2026-07-08-phase-{a,b}-results.md`). Phase A (snapshots, real judge): misses 2/3/4 FAIL at `a6ff1ca`, UJ-2.b correctly PASS (precision). Phase B (live seeded renders via `retrofit/phase-b-render.py` — scratch LEDGER_PATH, auto-migrations, Pydantic-valid seed, stub-mail capture, REAL compose path): miss 1 = UJ-2.b **UNREACHABLE** at `ef1c0c8` (the never-wired verdict, exactly as designed); miss 5 both halves = UJ-4.a FAIL (email sent during declared absence) + UJ-4.b FAIL (no return section) at `a6ff1ca`. Post-fix `82f4fe7`: ALL NINE end-states PASS. Per ADVISORY-UNTIL-PROVEN, only A5 adversarial now gates the blocking-default flip |
| A3.3 | Eval-framework regression entry for the retro-fit | 8 | done (repo-only, no version) | — | `scripts/acceptance-retrofit/run.mjs` (live re-execution, the H2.2 pattern — stronger than immutable replay): re-materializes the corpus (git-show snapshots + phase-b live renders, clones cached) + 7 REAL judge dispatches + asserts the full matrix. **FIRST FULL RUN GREEN**: identical verdicts to the A3.2 originals on an independent re-execution — detection 5/5, post-fix 0 false FAILs, precision 1/1 (matrix stable across runs). Gated as ship.md Step 4.8, conditional on judge-prompt/schema/workflow diffs; operator-workstation only (income-sources clone + real auth, Step 4.7 locality rationale). EPIC A3 COMPLETE |
| A4.1 | Pre-merge gate slot (verdict×tier paths) | 8 | done (v0.21.9) | v0.21.9 | Tier table (BLOCKING mode only — ADVISORY-UNTIL-PROVEN): critical walked-FAIL → `acceptance-fail` escalation pre-integration, branch durable; standard FAIL → pending fix-story proposal (manifest), run continues; advisory never blocks (test-pinned). 3 verdict×tier orchestrator-integration tests + **MATRIX CELL `journey-unreachable` UPGRADED + PASS live: never-wired → UNREACHABLE → acceptance-fail → NO merge — the full UJ-2 interception at the choke point** |
| A4.2 | Tier → finalization override (BOTH orchestrator config sites) | 8 | done (v0.21.9) | v0.21.9 | Critical ALL-PASS → integration downgraded merge→`acceptance.critical_pass_finalization` (branch default \| pr; config threaded through both schema copies + all 3 run.ts sites — H3.1 lesson): deliverable branch + verdict artifact await a HUMAN merge, run keeps moving. **NEW MATRIX CELL `journey-critical-pass-branch` PASS live** (story:finalized mode=branch, main not advanced). Report morning-review = existing H3.2 finalization section + A2.2 acceptance section |
| A4.3 | Gate cost telemetry | 8 | done (v0.21.9) | v0.21.9 | Judge dispatch tokens recorded per story+journey via addTokenUsage (phase `acceptance-judge`, create-story pattern, best-effort) → `substrate metrics`/report cost surfaces carry acceptance cost. EPIC A4 COMPLETE |
| A5.1 | Red-team review (12-item evasion catalog minimum) | 9 | done (v0.21.10) | v0.21.10 | Independent adversarial subagent (separate lineage) → 9 findings, `2026-07-08-red-team.md`. Spine + argv-injection defense confirmed SOLID (4 refuted). REMEDIATED Ship 9: F7 grounding (PASS excerpts must be verbatim — anti-injection), F2 (critical journeys must declare epic — schema), F4 (run-start SHA pins trusted loads in blocking mode), F3 (loud `acceptance-tamper-guard-disabled` warning), F1 (render-target-modified tripwire), F5 (final-sweep blocking-mode violation warning). F6/F8 accepted; F9 doc'd (canary-reserved). F1 structural close + F3 full close = accepted-risk pending A6/A5.4 (A7 blocking-flip gated on A6 canaries) |
| A5.2 | Evader-agent e2e cells (6 behaviors, all caught, CI-green) | 9 | done (v0.21.10) | v0.21.10 | Coverage: (a) claims-no-wire = `journey-unreachable` cell (now asserts acceptance-fail block) ✓; (c) spec-tamper cell ✓; (f) tag-omit = `journey-unclaimed` cell ✓; (e) `judge-injection` cell PASS live (data-not-instructions posture holds through a real dispatch); (b)/(d) render-side gaming = CONFIRMED F1 finding, documented not green-celled (the honest disposition — it's a real limit closed structurally only by A6 canaries) |
| A5.3 | Independent `/code-review ultra` (OPERATOR-TRIGGERED — session must request it) | — | **AWAITING OPERATOR** (asked 2026-07-08) | | Session cannot launch ultra. Requested: `/code-review ultra` on the v0.21.2→v0.21.10 arc (or the acceptance-gate diff). v0.21.0 precedent: ultra found a command-injection the red-team missed. Findings → new ledger rows before A7 |
| A5.4 | `--no-worktree` blocking-mode snapshot compare (red-team F3 full close) | — | todo | | Snapshot the 3 acceptance specs at run start, compare live specs against the immutable snapshot. Ship 9 shipped the loud `acceptance-tamper-guard-disabled` warning as interim. Design-doc-first |
| A6.1 | Canary engine (real-regression revert, auto-demote on miss) | 10 | todo | | |
| A6.2 | Precision instrumentation + demotion state machine | 10 | todo | | |
| A7 | FINAL GATE: live unattended real-agent run, planted never-wired journey caught, 0 false FAILs | — | todo | | Then operator decision: advisory→blocking flip |
| GC.1 | Grounding-contract check class (filed 2026-07-07 from the brief's operator addendum) | — | todo | | Field incident #2 (income-sources, same day): prompts mandating grounded output ("cite real URLs") while the grounding resource silently degrades → model squeezed into fabrication, shape-only checks launder it; survived 6 gates + ~1,400 tests. Scope: verification-pipeline check — (a) deterministic linter: grounding mandates in prompt templates paired with pre-call resource assertions + post-call cross-reference; (b) negative-path tests exist (empty dependency → honest skip); (c) degraded states emit counted operator-visible signals. Full rules: income-sources `_bmad-output/implementation-artifacts/grounding-guardrails.md`. Design-doc-first; slot after A5 or as its own mini-arc |

## Dependency notes
- Ship order 1→10 is the default. A3.1 (corpus pin) may start any time after Ship 2 — it is research, not code.
- A5.1 red-team runs between Ships 8 and 9; A5.3 ultra review after Ship 9. Neither is skippable; both gate A6/A7.
- `[dogfood-eligible]` stories may be dispatched via `substrate run --stories` after Ship 5 is verified — one per run, `--max-review-cycles 3`.

## Blockers
(none)

## Next session start here
Ships 1–8 SHIPPED (v0.21.2–v0.21.9). **Epics A0–A4 COMPLETE** (18 rows). The gate is functionally whole: registry→tags→coverage→contract→render→judge→verdicts→tier policy→finalization, retro-fit-proven (5/5, 0 false FAILs) with a 22-cell CI matrix and a regression harness gating judge changes. Remaining: **A5 adversarial (NOT skippable — the last ADVISORY-UNTIL-PROVEN condition)**: A5.1 red-team the 12-item evasion catalog (execution-plan.md; CONFIRMED evasions → new rows or dated accepted-risk doc `_planning/acceptance-gate/2026-XX-XX-red-team.md`), A5.2 evader-agent matrix cells (6 behaviors — (c) spec-tamper and (f) tag-omission already exist as cells; remaining: claims-without-wiring [journey-unreachable ≈ covers], dead-affordance, fixture-poisoning, judge-injection-live), A5.3 ASK THE OPERATOR to run /code-review ultra on the arc (v0.21.2–v0.21.9). Then A6 canaries (judge CLI is the surface; wire `substrate acceptance canary` + auto-demote) + GC.1 (grounding-contract class, operator addendum) + A7 final gate (live unattended real-agent run + operator blocking-flip decision).

## Decisions log
- 2026-07-07: Program created (design brief rev 2 + execution plan + this ledger). Executor = Claude session direct implementation; dogfood-eligible only after Ship 5 (the gate wires into the just-certified finalization/verification paths — no substrate-on-substrate there until the gate's own matrix cells exist).
- 2026-07-07: **ADVISORY-UNTIL-PROVEN pinned by test** — `acceptance.mode` defaults to `advisory`; the blocking default requires A3.2 retro-fit (5/5 + 0 false FAILs) + one live real-agent run + A5 adversarial phases. v0.21.1 lesson (stub-green ≠ release confidence) written into program structure.
- 2026-07-07: Placement — core in `packages/sdlc/src/acceptance/` mirroring `verification/`; artifacts external at `~/.substrate/acceptance/<name>-<hash8>/<run-id>/` (H4.2 symmetry). Trusted-tree reads for ALL judge/gate inputs (H7 posture) — a story adding an input without this is not done.
- 2026-07-07: Web-walkthrough driver (Midscene/Playwright) explicitly OUT of program scope — deferred pending A4.3 cost data + a real web-surface consumer.

## Session log
- 2026-07-08 (session 1, cont.): Ship 9 landed (v0.21.10) — Epic A5 adversarial code-complete. Independent red-team (separate lineage) → 9 findings; remediated F1–F5/F7 (F6/F8 accepted, F9 doc'd). Honest outcome on F7: a hard citation-grounding gate was prototyped, stress-tested on the real corpus, and DOWNGRADED to warn-only — it false-positived legitimate PASS ~50% and didn't actually defeat injection (the payload is itself groundable), so per design principle 4 a hard gate was net-negative. Retro-fit harness re-run GREEN with all remediation (5/5, 0 false FAILs). A5.3 ultra review requested from operator; A5.4 (no-worktree snapshot compare) filed.
- 2026-07-08 (session 1, cont.): Ship 8 landed (v0.21.9) — EPIC A4 COMPLETE: the verdict×tier table live at the finalization choke point (acceptance-fail block, critical-pass branch downgrade, standard-fail proposals, judge cost telemetry). Matrix 22 cells; the journey-unreachable cell now proves the full UJ-2 interception end-to-end on every CI run.
- 2026-07-08 (session 1, cont.): A3.3 done — retro-fit regression harness (`scripts/acceptance-retrofit/run.mjs`, ship.md Step 4.8 conditional gate). First full independent re-execution reproduced the A3.2 matrix verdict-for-verdict (7 real judge runs): the matrix is stable, not a lucky sample. EPIC A3 COMPLETE.
- 2026-07-08 (session 1, cont.): **A3.2 COMPLETE — THE GATE DETECTS ITS FOUNDING INCIDENT, 5/5.** Phase B live seeded renders (reconstructed the review's uncommitted harness as `retrofit/phase-b-render.py`): miss 1 judged UNREACHABLE at ef1c0c8 — the exact verdict class the design brief invented for it; miss 5 both halves FAIL at a6ff1ca (email sent during declared absence / no return section) and PASS at 82f4fe7. Combined with Phase A: 5/5 detection, 9/9 post-fix PASS, 1 precision check (already-fixed miss not flagged), 0 judge-prompt iterations across all 8 real-agent judge runs. Only A5 adversarial now gates the blocking-default flip.
- 2026-07-08 (session 1, cont.): Ship 7 landed (v0.21.8) — A3.1 corpus pinned (two-episode nuance: no single SHA has all 5 misses) + A3.2 PHASE A: the gate detected 3/3 snapshot-judgeable misses on REAL income-sources artifacts with a REAL judge and PRD-cited end-states, 0 false FAILs, zero prompt iterations — including correctly NOT flagging the already-fixed tap miss (precision). New `substrate acceptance judge` ad-hoc CLI (canary surface shipped early). Phase B (misses 1+5, live seeded renders) remains before A3.2 closes.
- 2026-07-08 (session 1, cont.): Ship 6 landed (v0.21.7) — A2.3 STAGE WIRING: the full acceptance loop runs live end-to-end (tagged story → contract render in worktree → separate-lineage judge → per-end-state verdicts → coverage → verdict HTML + manifest → merge). 3 new matrix cells (21 total, all green). LIVE REAL-AGENT JUDGE proof: a real claude dispatch judged the rendered farewell output PASS with verbatim evidence — the design brief's core mechanism works with a real model. Epic A2 COMPLETE.
- 2026-07-08 (session 1, cont.): Ship 5 landed (v0.21.6) — A2.1 judge dispatch (separate lineage pinned by test, evidence-mandatory schema, retry-once → acceptance-judge-invalid) + A2.2 verdict HTML artifact (XSS-escaped) + acceptance:started/rendered/verdict events + manifest verdicts + report section. Full suite 580/11,409; eval 100%; matrix 18/18.
- 2026-07-08 (session 1, cont.): Ship 4 landed (v0.21.5) — A1.3 AcceptanceSpecCheck (spec-tamper tripwire, all three specs + fixture-mutation warn) + live `spec-tamper` matrix cell (18 cells total). Epic A1 COMPLETE. Debug note: the check initially armed unconditionally and failed epic-25's mocked-fs e2e (trusted='' vs worktree=story-content read as DIVERGES) — guard now arms only when the trusted tree has a registry. Full suite 578/11,399; eval 100%; matrix 18/18.
- 2026-07-08 (session 1, cont.): Ship 3 landed (v0.21.4) — A1.1 acceptance contract (injection-safe by construction: argv-split-then-substitute, live child-argv proof) + A1.2 render executor (real-spawn tests: forensics, process-group timeout kill, determinism probe) + `acceptance-unrunnable` escalation with unclaimed > unrunnable > unwalked precedence. Full suite 577/11,390; eval 100%; matrix 17/17.
- 2026-07-07 (session 1, cont.): Ship 2 landed (v0.21.3) — A0.3 coverage audit end-to-end: pure ledger + per-story claim persistence + epic-close blocking hook at the finalization choke point + run-end manifest sweep + `acceptance.mode` config + `acceptance:coverage` event + defer CLI + matrix cell `journey-unclaimed` (17/17 matrix green). Full suite 575/11,365; eval 100%. Incidental harness hardening: fixed a checkGitDiffFiles implementation leak in worktree-merge-integration.test.ts that would have corrupted any test appended after line 1413. EPIC A0 COMPLETE — the never-wired-journey class is now structurally loud.
- 2026-07-07 (session 1): Ship 1 landed (v0.21.2) — A0.1 registry schema + trusted-tree loader + `substrate acceptance validate` CLI; A0.2 create-story journey tags end-to-end. Two ship-blocking catches during implementation: (1) `.substrate/acceptance/` was gitignored by the init writer — the registry could never be committed, so the trusted-tree loader would report `absent` forever and the gate would silently never fire (H1.1 profile-vanish class; fixed + repair pass + live check-ignore evidence); (2) strict typecheck caught an unguarded `story_file` read. Live smoke: real dispatch on the uv fixture with a committed registry — the agent emitted `journeys: [UJ-1]`, full cycle green. Full suite 574 files / 11,347 tests green; eval 100%.
- 2026-07-07: Program created. No stories started.
