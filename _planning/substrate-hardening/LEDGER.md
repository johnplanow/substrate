# Substrate Hardening Program — Ledger

**This file is the cross-session state machine.** Read it at session start; update it with every status change; ship ledger updates in the same commit as the code they describe. Statuses: `todo` | `in-progress (YYYY-MM-DD)` | `done (vX.Y.Z)` | `blocked` | `dropped (reason)`.

Program start: 2026-07-05. Plan: `execution-plan.md` (same dir). Strategy: `_planning/2026-07-05-substrate-bleeding-edge-plan.md`. Audit anchors: `_planning/2026-07-05-substrate-remediation-audit.md`.

## Status board

| ID | Story | Ship | Status | Version | Evidence / notes |
|----|-------|------|--------|---------|------------------|
| H0.1 | Commit-first + wip checkpoints + revision bracket | A | done (v0.20.139) | v0.20.139 | Tests: git-helpers 30/30 (+5), worktree-merge-integration 21/21 (+6), reconcile 27/27 (+2), test:fast 10220. LIVE (uv fixture run a9125656): full bracket `baseline_sha fb02fea`+`commit_sha 48812c8` in manifest; commit at dev-story-complete second, 33s before review complete; `uv run pytest` 2/2 on merged main. CI+Publish green 2026-07-05 |
| H0.2 | Finalization unified across verdict paths | A | done (v0.20.139) | v0.20.139 | finalizeStory() shared by SHIP_IT + both auto-approve sites; finding-#1 regression test green. LIVE: full SHIP_IT cycle on uv fixture (merge, cleanup, completed). CI+Publish green 2026-07-05 |
| H0.3 | Worktree removal dirty-guard | B | done (v0.20.141) | v0.20.141 | decideWorktreeRemoval (pure) + inspectWorktreeRemovalSafety; guards dirty trees AND unmerged branch commits; CLI --force; orphan sweep preserves unsafe. LIVE (bundled dist on uv fixture): cleanup of dirty worktree REFUSED naming precious.py, work preserved; --force discarded worktree+branch. 11 new tests |
| H0.4 | Dispatch forensics + auth classifier + halt | B | done (v0.20.141) | v0.20.141 | detectClaudeAuthFailure (8 field-verified signatures, core) + CLAUDE_AUTH_FAILURE_HINT; source classification at create-story schema-fail (exit-0 refusals); auth-failure = FATAL decision type (halts under every --halt-on); triggerAuthFailureHalt sweeps PENDING→auth-failure-halt; buildPlanningCommand env-scrub parity. 10 new tests (7 classifier + 3 halt-wiring incl. no-false-halt). Live auth repro deliberately deferred to H2.2 stub adapter (cannot safely break real CLI auth on the operator workstation) |
| H0.5 | Escalation parent-leak capture on every escalation | B | done (v0.20.141) | v0.20.141 | From field finding #20 (income-sources 6-1: escalation abandoned a COMPLETE implementation uncommitted in the PARENT tree). emitEscalation now runs detectWorkOutsideWorktree; dirty parent → PARENT-TREE LEAK message (files named) in escalation issues/escalation_detail + orchestrator:story-warn event. Regression test green. Root fix = H4; this is the tourniquet |
| H1.1 | One project model + uv + profile-in-worktrees | C | done (v0.20.142) | v0.20.142 | uv detection (detect.ts + schema + install-command: uv.lock → uv sync/uv run pytest/uv add); profile negation in gitignore writer + repair pass; profile always copied into worktrees (createWorktree); BuildCheck profile-priority + non-Node-before-package.json (finding #12 flip dead); shouldRunTscCheck explicit-language tier. LIVE: fixture re-init produced buildTool: uv profile; gitignore repaired. NOTE: full 4-detector consolidation into ONE module deferred (package boundary) — BuildCheck reads the profile FILE; residual dedup filed under H1.6 notes |
| H1.2 | Real-suite gate + kill Node prompt fallback | C | done (v0.20.142) | v0.20.142 | TestSuiteCheck (Tier A, after build): runs profile testCommand in worktree, 300s cap, fail-on-red; context.testCommand/buildCommand plumbed from profile in assembleVerificationContext; dev prompt verify_command = pack → profile buildCommand → OMIT (npx turbo build fallback DELETED). LIVE (fixture): green suite pass via `uv run pytest`; red suite fail naming test_farewell. GUARDRAILS from the 2026-07-05 near-OOM incident: recursion guard (auto-detected suites never run under VITEST; SUBSTRATE_ALLOW_NESTED_TESTS=1 opt-out), 2GB heap cap on spawned suites, ''-means-skip contract pinned by regression test. 11 new tests |
| H1.3 | Probe env fidelity | D | done (v0.20.143) | v0.20.143 | buildProbeEnv: project-local .venv/bin prepended to PATH + VIRTUAL_ENV set in the default probe executor (quoting-free, tool-agnostic); probe-author prompt gains {{project_env}} with the profile's canonical invocation (uv run …). LIVE (uv fixture): bare `python -c 'import pytest'` probe fail (`python: not found`) WITHOUT shaping → pass (9.1.1) WITH shaping. +3 executor tests incl. which-binary-ran proof |
| H1.4 | Net-new-implementation gate | D | done (v0.20.143) | v0.20.143 | classifyImplementationDiff (pure): implementation = any file outside _bmad-output/ + .substrate/; success story with artifact-only ground-truth diff → ESCALATED `no-implementation` (before commit-first; H0.1 checkpoint still preserves the artifact). Finding-#13 regression test green (spec-only diff + self-reported tests-pass → escalated, no merge); docs-only stories outside artifact trees stay legal. +7 tests |
| H1.5 | Contamination gate + commit denylist | E | done (v0.20.144) | v0.20.144 | ContaminationCheck (Tier A, after test-suite): fails on foreign-language sources vs profile languages (monorepo polyglot passes naturally), JS toolchain manifests on non-JS projects (the #12 flip trigger), and droppings (node_modules/.venv/__pycache__ always; dist/ on non-JS). Ground-truth changedFiles plumbed through context. Commit-side denylist in commitDevStoryOutput (belt-and-braces, warn-logged). Findings #12/#16/#18 regression tests green (incl. the exact 1,885-file shape). +19 tests |
| H1.6 | Self-report demotion + Gherkin + BuildCheck order | E | done (v0.20.145) | v0.20.145 | tests-claim-mismatch finding when agent claims pass over a red suite (TestSuiteCheck); fidelity check prefers ground-truth changedFiles over self-report (benefit-of-doubt only when NO signal exists); Gherkin AC inference (Scenario headings, else Given openers → positional ACs — finding #8); ac-context-missing message now states enforcement is skipped. BuildCheck ordering landed v0.20.142. +4 tests |
| H1.7 | Reward-hack tripwire | E | done (v0.20.145) | v0.20.145 | TestMutationCheck (warn, Tier A): flags stories that MODIFY pre-existing test files (tracked-diff capture `checkGitModifiedTrackedFiles`, best-effort); new tests silent; cross-language test-path idioms. +6 tests |
| H1.8 | Story-artifact path containment | E | done (v0.20.145) | v0.20.145 | Worktree-mode gate: an ABSOLUTE create-story `story_file` outside the worktree → ESCALATED `create-story-outside-project` naming the stray path (relative paths are inside-by-construction). Live-capture regression test green ($HOME artifact shape). Origin: 2026-07-05 live capture during Ship A evidence run |
| H2.1 | Fixture consumer repos (py-uv / node-ts / go) | F | done (repo-only, no version) | — | fixtures/consumer-{python-uv,node-ts,go}/: real tiny suites (pytest / node:test / go test), bootstrap.sh each (all three green locally), epics.md with one clean + one verification-trip story each, committed uv.lock for detection-without-bootstrap. Inert to substrate's suite (509 files unchanged). Not published content — shipped as repo commit without a tag |
| H2.2 | Stub-agent pipeline e2e in CI | F | done (v0.20.146) | v0.20.146 | StubAdapter (core, double-gated: registry env + healthCheck script check) + scenario script + matrix harness (scripts/e2e-fixture-matrix/). FULL MATRIX GREEN 8/8: py success (real uv pytest post-merge), zero-impl→no-implementation, contamination→scope-contamination fail+no merge, red-suite→test-suite fail+tests-claim-mismatch+durable branch, auth-error→fatal halt, no-file, node+go success. HARNESS CAUGHT A REAL BUG pre-ship: Tier-A RETRY verification lacked ground-truth changedFiles → a contamination FAIL retried into a blind context, passed, and MERGED; fixed with recaptureChangedFiles (baseline..HEAD ∪ dirty). CI job `fixture-matrix` REMOTE-GREEN (run 28764967080; three runner fixes en route: YAML orphan line, git identity, git init -b main — the last exposing the H4.2 baseBranch hardcode in the wild). H0.4's deferred live-auth evidence CLOSED (auth-error cell) |
| H2.3 | Nightly live smoke | G | done (repo-only, no version) | — | scripts/nightly-live-smoke/: run.mjs (fresh workspace, real dispatch, assertions, ~/.substrate-smoke/history.log verdict line, failure preservation) + systemd user service/timer pair + README. ENABLEMENT = OPERATOR STEP (systemctl --user enable — commits nightly dispatch quota). Evidence: one live execution end-to-end — it correctly caught, preserved, and logged a GENUINE live failure (fix agent deleted the story artifact → ENOENT → escalation), with H0.1's feat+wip commits keeping all work durable on the branch. The smoke works; FIRST PASS 2026-07-06 (post-H5.5 dist, external worktree base): full live cycle green — history.log `2026-07-06T13:29 PASS` |
| H2.4 | Eval-corpus regression cases (field findings) | G | dropped (superseded by H2.2) | — | The fixture-matrix cells ARE the named regression encodings of findings #10/#11/#12/#13/#16/#17/#18 — live re-execution in CI on every PR, strictly stronger than Tier-2a immutable replay (which cannot cover cross-repo field runs; the income-sources story_metrics live in that repo's Dolt, not here). Findings #1/#14/#15 are covered by unit/integration regression tests shipped with H0.1/H0.2/H0.5 |
| H3.1 | finalization.mode: merge\|branch\|pr | H | done (v0.20.147) | v0.20.147 | Config `finalization.mode` + CLI `--finalization <mode>` (invalid → exit 2), threaded to BOTH orchestrator config sites (impl-only path AND full-pipeline — the impl-only miss was caught LIVE by the new matrix cell, not by unit tests). branch/pr STOP before merge: worktree removed with new `cleanupWorktree keepBranch` (unmerged-commits guard relaxed for the kept branch ONLY, dirty-tree guard intact — later unflagged cleanups still refuse via H0.3, so the deliverable branch is structurally protected). pr = push + `gh pr create`, DEGRADES to branch on any failure (never blocks). Matrix cells `branch-mode` (main NOT advanced, feat on branch, worktree gone) + `pr-degrade` (no remote → degraded, no pr_url) GREEN; full py matrix 8/8. +6 unit/integration tests |
| H3.2 | Lifecycle events + report rendering | H | done (v0.20.147) | v0.20.147 | Internal events `orchestrator:story-committed/-merged/-finalized` → NDJSON `story:committed/merged/finalized` (event-types + help-agent metadata + run.ts emitter wiring). `finalization {mode,branch,sha,pr_url}` persisted to per-story manifest state (single schema source packages/sdlc). `substrate report`: JSON carries per-story finalization; human render adds "Finalization (deliverable branches — NOT merged)" section for branch/pr stories — verified EMPIRICALLY on a live branch-mode run (section rendered with branch+sha). pr-mode row without pr_url = documented degraded marker |
| H3.3 | Merge preconditions (parent-clean, ff-only, fatal start-branch) | I | done (v0.20.148) | v0.20.148 | AC1: Step-0 parent-clean precondition in runMergeToMain — parent dirt ∩ story diff → `parent-tree-dirtied-by-run` naming files (finding #15 truthful escalation), no merge, branch preserved; unrelated parent dirt proceeds; rename-porcelain handled. AC2: `finalization.merge_strategy` ff-only\|three-way, DEFAULT FLIPPED to ff-only — diverged base now escalates `ff-only-merge-not-possible` naming the three-way remedy instead of silently synthesizing a merge commit. **BEHAVIOR CHANGE + H6 NOTE: concurrent multi-story runs need `merge_strategy: three-way` (later stories can't FF past earlier merges) or sequential dispatch; the H6 gate run must set one of these.** AC3: start-branch capture failure now FATAL at run start under worktrees (was: warn + run-long silent hand-land factory); stays non-fatal under --no-worktree. merge-to-main mock rerouted (status/three-dot-diff answer presets, not the blind queue) to keep pre-H3.3 queue semantics. +11 tests (5 unit, 6 integration) |
| H3.4 | Epic gate hook | I | done (v0.20.148) | v0.20.148 | `finalization.epic_gate_command`: LAST story of an epic (all run-scope siblings terminal) must pass the command before merge/pr finalization; non-zero → `epic-gate-failed` escalation carrying the command output (4000-char cap), branch preserved; branch mode skips. Matrix cells `epic-gate-pass` (marker file proves gate ran, story still merges) + `epic-gate-fail` (EPIC-GATE-RED output surfaced, no merge, branch durable) GREEN via configAppend workspace hook. Known limitation recorded: two same-epic stories finalizing concurrently both see a non-terminal sibling → gate skipped; exact under sequential dispatch/dependency-serialized epics. +3 integration tests |
| H4.1 | Git-state scoping at spawn (env scrub + ceiling) | J | done (v0.20.149) | v0.20.149 | AC1: dispatcher scrubs PWD/OLDPWD/INIT_CWD/GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE/GIT_COMMON_DIR from every child env + sets GIT_CEILING_DIRECTORIES to the worktree parent (adapter cmd.env still wins). AC2: workingDirectory REQUIRED for worktree-coupled taskTypes (create-story/dev-story/code-review/minor-fixes/major-rework/build-fix/test-plan/test-expansion/probe-author) — missing → failed DispatchResult before spawn, no slot leak; planning types keep the cwd fallback. AC3 EMPIRICAL: the stub agent now hard-fails (exit 78) on any leaked git env, missing ceiling, or `git rev-parse --show-toplevel` ≠ worktree — EVERY matrix cell enforces this live on every CI run. +3 dispatcher unit tests; 62 pre-existing test dispatches updated to name their worktree |
| H4.2 | External worktree base + baseDirectory hardcode fix | J | done (v0.20.149) | v0.20.149 | AC1: gitUtils.createWorktree honors baseDirectory (the create-here/clean-there hardcode is dead); getWorktreePath/getOrphanedWorktrees are absolute-base-aware (resolve, not join). AC2: `worktree.base: external\|in-repo`, DEFAULT external → `~/.substrate/worktrees/<name>-<hash8>/` — no parent repo above the worktree, composing with H4.1's ceiling. Resolution is a single exported `resolveWorktreeBaseDirectory` that reads config.yaml directly (resolveEpicsPathOverride pattern) so orchestrator + `substrate merge` + `substrate worktrees` resolve identically without threading. MIGRATION NOTE in help-agent output. BONUS (live CI evidence from H2.2): createWorktree defaults to the repo's CURRENT branch (rev-parse) instead of hardcoded 'main' — the master-default runner failure class is dead. AC3 EMPIRICAL: full 12-cell matrix green under the external default; worktreePath observed at ~/.substrate/worktrees/…; harness cleans the external base on PASS (0 orphans after a full run). +7 resolver tests |
| H4.3 | Permission-scoped dispatch experiment + decision | K | done (v0.20.150) | v0.20.150 | AC1: `dispatch.permission_profile: skip\|scoped` — scoped generates a per-worktree settings file (written at the worktree PARENT — the external base, never committable; mutation allowed only under the worktree via allow rules; NO deny rules since deny wins over allow and any parent pattern would shadow the worktree beneath it) and swaps `--dangerously-skip-permissions` for `--permission-mode acceptEdits --settings <file>`. Headless -p denies un-allowed tools visibly — no interactive stall class. Threaded config→both dispatcher sites→AdapterOptions; claude adapter honors, others ignore. AC2 MEASURED (2026-07-06, real claude dispatches on the uv fixture, 1 story/arm): skip exit 0, 175.8s, merged, 0 permission signals; scoped exit 0, 165.5s, merged, 0 permission signals, 0 escalations. AC3 DECISION: **keep default `skip`** — scoped is validated-viable on a clean single story but n=1/arm with no evidence on complex multi-file stories; revisit the flip at H6 where the ≥10-story batch can run `scoped` as its profile and provide the volume evidence. +2 adapter unit tests |
| H4.4 | Container-ready seam (types + doc + rule) | K | done (v0.20.150) | v0.20.150 | `SpawnCommand.executionMode?: 'spawn'\|'container'` typed on BOTH copies (packages/core + src — Epic 79 dual-schema pair) + ISpawnCommand; dispatcher REJECTS non-spawn modes with a failed DispatchResult (no silent host fallback). Seam doc `docs/2026-07-06-container-execution-seam.md` maps the two bind-mount seams (external worktree base + the single spawn site). Conformance rule enforced as a TEST: `src/__tests__/worktree-path-conformance.test.ts` — git-grep for `.substrate-worktrees` outside an 8-file allow-list fails the suite with use-the-manager instructions. +2 tests |
| H5.1 | Finding #7 root cause + fix | L | done (v0.20.151) | v0.20.151 | AC1 REPRODUCTION ATTEMPTED, DOES NOT REPRODUCE on v0.20.150: fixture epics extended to 4 stories (1.3 whisper, 1.4 greet_many); 3 consecutive REAL multi-story runs (1-1,1-3,1-4 each — 9 create-story dispatches sequenced after prior full cycles, the exact refined-repro shape from run `0b56df1d`): 9/9 stories verified+merged, zero no-file, zero escalations (~10 min/run). Both named hypotheses REFUTED as live-blocking causes on current code: OAuth-race — the H0.4 classifier saw zero auth signatures across all runs; NODE_OPTIONS 512MB heap cap — every live experiment this program ran (H4.3 both arms, H2.3 smoke, these 3 runs) inherited the cap and completed full dev cycles. CLOSED as fixed-by-intervening-hardening (most plausible killers of the original mechanism: H4.1 env scrub — child git/PWD state no longer leaks; H4.2 external worktree base — teardown no longer mutates inside the repo; H0.4 no-file classification). Repro harness preserved at scratchpad h51-repro.mjs shape for re-use if the signature ever returns |
| H5.5 | Story-artifact resilience (branch-HEAD recovery + story-file-missing classification) | L | done (v0.20.151) | v0.20.151 | `recoverStoryFileFromBranch` (git-helpers): reads `git show HEAD:<rel>` in the worktree, restores the file to the working tree, containment-guarded (outside-worktree paths refused). Wired into code-review AND dev-story read paths — the live-smoke incident shape (fix agent deleted the committed artifact) now self-heals. When recovery ALSO fails (gone from branch HEAD too), the orchestrator classifies `story-file-missing` with a check-the-branch message instead of the misleading `consecutive-review-timeouts`. +4 real-git integration tests (deleted→recovered+restored, absolute path, never-committed→undefined, containment) |
| H5.2 | Field residue #2/#3/#4/#5/#9 | L | done (v0.20.151) | v0.20.151 | #2: ROOT CAUSE was a lying banner — init advertised /substrate-run,/substrate-supervisor,/substrate-metrics under .claude/commands/ but NOTHING ever wrote them (bmad generator populates .claude/skills/ only); new `scaffoldSubstrateSlashCommands` writes the 3 templated commands (best-effort, user files preserved, pre-existing/gitignored .claude/ fine — empirically verified both shapes). #3: efficiency_scores DDL gained token_density_sub_score + cold_start_turns_excluded + forward ALTERs; NEW static DDL↔INSERT parity test closes the class (the round-trip suite ran on InMemoryAdapter which doesn't enforce columns — why it never caught this). #4: report header cost falls back to Σ per-story cost_usd when run_total=0 (subscription routing) — verified empirically ($0.0000→$0.3786 on the finding's exact manifest shape). #5: template documents init droppings (AGENTS.md/GEMINI.md/packs/ are intentional, track-or-ignore guidance). #9: probe summary now `N ran (X passed, Y failed), M deferred (sandbox=twin)` instead of the inflated raw count; +2 regression tests |
| H5.3 | Docs-match-behavior gate | M | done (v0.20.151) | v0.20.151 | Consumer template REGENERATED for the program's behavior changes (external worktree base, commit-first/wip bracket, full finalization section: 3 modes, ff-only default + three-way concurrency requirement, parent-clean escalation, lifecycle events, epic gate, --finalization row). Gate = `docs-match-behavior.test.ts`: pins finalization modes against the config enum, ff-only/three-way/external-base/commit-first claims, and validates every event name the template cites against EVENT_TYPE_NAMES (no phantom events). Ship checklist rule added to .claude/commands/ship.md Step 5. help-agent escalation-reason list updated with the 4 new reasons (H3.3×2, H3.4, H5.5) |
| H5.4 | (stretch) Architecture-conformance check | — | deferred (2026-07-06) | — | Explicitly deferred past program close per plan ("may slip past H6"). Design-doc-first work; candidate for a future arc alongside the exit-code and scoped-default follow-ups from the H6 report |
| H6 | FINAL GATE: unattended ≥10-story batch, zero hand-lands | — | **done — GATE PASSED (2026-07-06)** | — | 10-story unattended batch on the uv fixture (v0.20.151, three-way + scoped profile, --halt-on none --non-interactive), 34 min: 9/10 verified+merged, 1/10 (1-2) failed TRUTHFULLY (`ac-missing-evidence` on the fixture's un-evidenceable harness-note AC — gate fired correctly, work durable on the branch). ZERO hand-lands, ZERO parent-tree leaks (porcelain clean), ZERO false-completes; `uv run pytest` green on main after 9 sequential three-way merges. Full report: `_planning/2026-07-06-h6-final-gate-field-feedback.md`. Two follow-ups filed there (exit-code 2 overstatement for verification-failed; scoped-default flip evidence now sufficient) |

## Dependency notes
- Ship order A→M is the default; H2 (F/G) may interleave from Ship B onward and SHOULD land before Ship E completes (later ACs cite fixture evidence).
- H3+ stories marked `[dogfood-eligible]` in the plan may be dispatched via substrate itself once Ships A+B are verified in a real run.

## Blockers
(none)

## Next session start here
**PROGRAM COMPLETE (2026-07-06).** All rows resolved; H6 gate PASSED; H5.4 stretch deferred. Follow-up backlog (new arc, not this program): exit-code classification for verification-failed stories (H6 obs #1), scoped-permission default flip (H6 obs #2 — evidence sufficient), H5.4 architecture-conformance design doc, Direct-API Codex adapter (pre-existing), Epic 79 fleet-data consolidation (pre-existing).
- H4.2 note: the fixture-matrix runner exposed the `main` baseBranch hardcode live (`git worktree add … invalid reference: main` on a master-default repo) — carry that evidence into H4.2's fix.

## Decisions log
- 2026-07-05: **WORKSTATION-PROTECTION GUARDRAILS (operator demand after near-OOM).** TestSuiteCheck's first implementation let `testCommand: ''` fall through to profile detection → recursively spawned substrate's own `npm test` inside the test suite → 25 orphaned vitest processes, near-OOM. Product guards added (recursion guard + heap cap + skip-contract fix); operator guards added to CLAUDE.md (ps-based vitest count before/after test-spawning work, never ambient-detect against substrate's own repo, full npm test before verification-path pushes).
- 2026-07-05: Executor = Claude session direct implementation; substrate-on-substrate only for dogfood-eligible stories after H0. Default `finalization.mode` stays `merge` for backward compat until H3.1's init heuristic ships.
- 2026-07-06: **Verify `git diff --cached --stat` before every ship commit.** The first v0.20.144 feature commit staged only 1 of 11 files — a bad pathspec in a multi-path `git add` (masked by 2>/dev/null) fails the WHOLE add silently. Tag was pulled before publish; re-tagged clean at 454fa3f. Never suffix `git add` with 2>/dev/null; always inspect the staged stat before committing a ship.
- 2026-07-05: **Full `npm test` (not test:fast) is mandatory before every hardening ship.** v0.20.140 burned because test:fast excludes e2e/integration — four git-utils mock factories missing the new H0.3 export failed only in CI (the documented v0.20.80 lesson, re-learned). Burned-tag recovery per ship.md: tag deleted, re-shipped as v0.20.141.

## Session log
- 2026-07-06 (H7 remediation + independent review + CONCLUSIVE H6): after the hardening program's first H6 pass, an adversarial red-team (28 gate evasions) + independent `/code-review ultra` (command-injection + a disclosure-gate false-positive) drove the H7 trust-boundary remediation (v0.20.152–156) and minor bump v0.21.0. A live smoke on 0.21.0 caught a CRITICAL regression (disclosure gate compared absolute vs relative paths → every real story false-escalated since v0.20.153) → hotfix v0.21.1. **CONCLUSIVE H6 GATE (v0.21.1): 10/10 clean stories merged, exit 0, gatePass=true, zero escalations/hand-lands/leaks, suite green, disclosure gate healthy at real-agent scale.** Write-up: `_planning/2026-07-06-h6-conclusive-gate.md`. Durable lesson: the stub matrix shares the operator blind spot when the stub doesn't mirror real-agent output shape — a live real-agent run is mandatory for release confidence.
- 2026-07-06 (session 1, FINAL): **H6 GATE PASSED — PROGRAM COMPLETE.** 10-story unattended live batch: 9 merged, 1 truthful verification failure with durable branch, zero defect-class occurrences. Report at `_planning/2026-07-06-h6-final-gate-field-feedback.md`. Program totals: 27 rows resolved (1 stretch deferred), 12 versions (v0.20.139, 141–151), 2 tag recoveries / 0 bad publishes, fixture matrix 12 cells CI-enforced, nightly smoke live with first PASS.
- 2026-07-06 (session 1, close 7): Ships L+M landed together (v0.20.151) — H5.5 branch-HEAD recovery + story-file-missing, H5.1 closed (3× consecutive multi-story live runs green, 9/9 stories — finding #7 does not reproduce; both hypotheses refuted as live causes), H5.2 all five residue items (#2 lying-banner root cause + real /substrate-* commands, #3 DDL drift + parity test, #4 header cost fallback, #5 droppings doc, #9 probe summary split), H5.3 template regenerated + docs-match-behavior gate + ship-checklist rule. Nightly smoke FIRST PASS (post-H5.5 dist, external base). All rows H0–H5 now resolved except stretch H5.4; only H6 remains.
- 2026-07-06 (session 1, close 6): Ship K landed — H4.3 permission-scoped dispatch + H4.4 container seam (v0.20.150). Epic H4 COMPLETE. The scoped experiment ran live (2 real dispatches): both profiles green, comparable wall-clock, zero permission stalls — default stays `skip` until H6 provides batch-volume evidence. Full suite 564/11258, matrix 12/12, eval 100%. Session totals: 11 versions published (139/141–150), 25 rows resolved; remaining: H5.1/H5.2/H5.5/H5.3 (+H5.4 stretch) → H6 gate.
- 2026-07-06 (session 1, close 5): Ship J landed — H4.1 git-state scoping + H4.2 external worktree base (v0.20.149). Two default flips in one ship, both isolation-hardening: agents get a scrubbed env with a git ceiling, and worktrees now live OUTSIDE the parent tree by default (`worktree.base: in-repo` restores legacy). The stub agent now enforces H4.1 live in every matrix cell (exit 78 on leak). Also killed the baseBranch 'main' hardcode with current-branch detection (live CI evidence from the H2.2 arc). Full suite 563/11254, matrix 12/12 external, eval 100%. Session totals: 10 versions published (139/141–149), 23 rows resolved.
- 2026-07-06 (session 1, close 4): Ship I landed — H3.3 merge preconditions + H3.4 epic gate (v0.20.148). ff-only is now the DEFAULT merge strategy (never-surprise-merge); three-way is config opt-in and REQUIRED for concurrent multi-story runs — flagged on the H6 row's path. Matrix now 12 cells (epic-gate-pass/fail added), full suite 562/11243, eval 100%. Session totals: 9 versions published (139/141–148), 21 rows resolved.
- 2026-07-06 (session 1, close 3): Ship H landed — H3.1 finalization modes + H3.2 lifecycle events/report (v0.20.147). The `branch-mode` matrix cell caught a REAL threading gap pre-ship (flag wired into the full-pipeline orchestrator site but not the impl-only `--stories` site — merge ran despite `--finalization branch`); second live catch by the H2.2 harness in two ships. Full suite 562/11229 green (one help-agent metadata sync fixed, one non-interactive ENOTEMPTY teardown flake passed on isolated re-run); eval 100%; matrix 8/8 + 2 new cells. Session totals: 8 versions published (139/141–147), 19 rows resolved.
- 2026-07-05: Program created (audit + strategy + execution plan + this ledger). No stories started.
- 2026-07-05 (session 1): H0.1 + H0.2 implemented with tests (Ship A pending live evidence). Incidental observation: `substrate init --yes` on a fresh uv fixture DID scaffold `.claude/commands/` correctly — field finding #2 may be conditional on pre-existing `.claude/` state (repro note for H5.2, AC1).
- 2026-07-06 (session 1, close 2): Ship F landed — H2.1 fixtures + H2.2 stub-agent matrix e2e (v0.20.146 published; fixture-matrix CI job remote-green after 3 runner fixes). The harness caught a REAL merge-gate bypass pre-ship (retry verification without ground truth) — the matrix's raison d'être proven on day one. Session totals now: 7 versions published (139/141/142/143/144/145/146), 15 stories done (all of H0+H1, H2.1+H2.2).
- 2026-07-06 (session 1, close): Ship E pt.1 landed (v0.20.144 CI+Publish green after a tag-recovery: first tag carried a 1-file commit due to a silent git-add pathspec failure; pulled pre-publish, re-tagged at the complete commit). Session total: 5 ships published (v0.20.139/141/142/143/144), 10 stories done (H0.1–H0.5, H1.1–H1.5), 2 new stories filed (H0.5 from field #20, H1.8 from live capture), 3 process disciplines added (full-suite gate, staged-stat verification, workstation-protection rules in CLAUDE.md).
- 2026-07-05 (session 1, cont. 2): Ship C landed (v0.20.142, CI+Publish green). Ship D (H1.3+H1.4) implemented + shipped as v0.20.143. Full suite 559/11191 green, eval 100%, zero stray vitest before/after.
- 2026-07-05 (session 1, cont.): Ship A → v0.20.139 (CI+Publish green). Ship B → v0.20.140 BURNED (e2e mock gap, caught in CI) → fixed → v0.20.141. H1.1 in progress: uv detection (canonical detector + schema + install-command), profile gitignore negation + repair, profile copy at createWorktree, BuildCheck profile-priority + non-Node-before-package.json ordering, shouldRunTscCheck language tier. One flaky full-suite test observed (1/11170, non-reproducing, name not captured) — watch on future full runs.
