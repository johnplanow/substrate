/**
 * ProjectProfile type re-exports.
 *
 * Story 37-2 imports ProjectProfile from this file as per its interface
 * contracts. The canonical type definitions live in types.ts.
 */
export type { Language, BuildTool, PackageEntry, ProjectProfile } from './types.js'
