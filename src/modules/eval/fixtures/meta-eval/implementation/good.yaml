name: implementation-good-cli-task-tracker
description: Real captured implementation-phase output for story 3-1 (Configurable storage path via flag and environment variable)
phase: implementation
notes: |
  Regenerated from real pipeline output per deferred-work G8.a. Unblocked
  by G11 — the dev-story dispatch capture now writes to phase_outputs so
  eval can read the real impl-phase text instead of grading against a
  hand-authored stub.

  Source: substrate run 69549751 (cli-task-tracker concept, G11-instrumented).
  Representative dispatch: 3-1:dev-story (1175 chars).
  Story spec in reference_context below is the REAL solutioning output for
  story 3-1, extracted from the decisions table for the same run.

  Pre-G8.a this fixture was hand-authored with a synthetic Story S2
  (`tt add` command) because dev-story dispatches ran outside the G2
  step-runner capture path. See deferred-work.md G8.a and G11 for history.

  bad.yaml retains the old hand-authored Story S2 reference_context and
  output for now. It still works structurally but no longer matches this
  good fixture's story (a length-matched subtle-bad for story 3-1
  is a follow-up calibration item — see G8.a deferred notes).
reference_context: |
  Story 3-1 — Configurable storage path via flag and environment variable

  Description:
    Implement the three-tier path resolution in PersistentPreRunE on the cobra root command: the explicit --file or -f flag takes highest precedence, followed by the TASKS_FILE environment variable, followed by the default tasks JSON file in the user home directory. Resolve the final path through filepath.Abs then filepath.Clean before any use. Store the resolved path in an AppConfig struct passed by value to all subcommands so resolution logic is never duplicated. Reject paths that resolve to sensitive system prefixes by returning a typed ErrUnsafePath sentinel with a clear error message and exit code 1. This implements FR-11 and the configurable-path aspect of FR-6.

  Acceptance criteria:
    - AC1: When --file is provided it is used as the storage path, taking precedence over any TASKS_FILE environment variable or the default path
    - AC2: When TASKS_FILE is set and --file is not provided, the environment variable value is used as the storage path
    - AC3: When neither --file nor TASKS_FILE is set, the storage path defaults to the tasks JSON file in the user home directory
    - AC4: All subcommands read from and write to the resolved path without each one independently repeating the resolution logic
    - AC5: A path value containing directory traversal sequences is cleaned to its absolute form before any file I/O is attempted
    - AC6: A resolved path that matches a sensitive system directory prefix returns a clear error message on stderr and exits with code 1
    - AC7: Unit tests verify all three precedence tiers return the correct resolved path
    - AC8: A test creates a task using --file pointing to a temp directory and then reads it back using the same path, confirming data is accessible

  Priority: should
output: |
  ```yaml
  result: success
  ac_met:
    - AC1
    - AC2
    - AC3
    - AC4
    - AC5
    - AC6
    - AC7
  ac_failures: []
  files_modified:
    - /Users/justin/substrate-impl-capture/go.mod
    - /Users/justin/substrate-impl-capture/go.sum
    - /Users/justin/substrate-impl-capture/internal/config/config.go
    - /Users/justin/substrate-impl-capture/internal/config/config_unix.go
    - /Users/justin/substrate-impl-capture/internal/config/config_windows.go
    - /Users/justin/substrate-impl-capture/internal/config/config_test.go
    - /Users/justin/substrate-impl-capture/cmd/tasks/main.go
    - /Users/justin/substrate-impl-capture/cmd/tasks/list.go
    - /Users/justin/substrate-impl-capture/tests/integration/path_test.go
    - /Users/justin/substrate-impl-capture/internal/task/task.go
    - /Users/justin/substrate-impl-capture/internal/task/priority.go
    - /Users/justin/substrate-impl-capture/internal/task/id.go
    - /Users/justin/substrate-impl-capture/internal/storage/errors.go
    - /Users/justin/substrate-impl-capture/internal/storage/store.go
    - /Users/justin/substrate-impl-capture/internal/storage/json_store.go
    - /Users/justin/substrate-impl-capture/internal/display/display.go
  tests: pass
  ```

