/**
 * Performance Aggregates — shim re-exporting from @substrate-ai/core (Story 41-7).
 */
export type {
  PerformanceAggregates,
  AgentPerformanceMetrics,
  TaskTypeBreakdownResult,
} from '@substrate-ai/core'
