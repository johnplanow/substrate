/**
 * Re-export shim for ConfigSystem interface and ConfigSystemOptions.
 * These types have moved to @substrate-ai/core.
 */
export type { ConfigSystem, ConfigSystemOptions } from '@substrate-ai/core'
