/**
 * Re-export shim for ConfigWatcher.
 * Implementation has moved to @substrate-ai/core.
 */
export { createConfigWatcher, flattenObject, computeChangedKeys } from '@substrate-ai/core'
export type { ConfigWatcher, ConfigWatcherOptions } from '@substrate-ai/core'
