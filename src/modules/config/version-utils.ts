/**
 * Re-export shim for version utility functions.
 * Implementation has moved to @substrate-ai/core.
 */
export {
  parseVersion,
  isVersionSupported,
  getNextVersion,
  formatUnsupportedVersionError,
} from '@substrate-ai/core'
