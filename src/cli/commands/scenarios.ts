/**
 * `substrate scenarios` command — factory scenario management.
 *
 * Delegates to `registerScenariosCommand` from @substrate-ai/factory, which
 * registers the `scenarios run [--format json|text]` subcommand.
 *
 * Story 44-5.
 */

export { registerScenariosCommand } from '@substrate-ai/factory'
